# Kalshi API credentials
# Get these from https://kalshi.com/account/api
KALSHI_API_KEY_ID=
KALSHI_API_KEY_RSA=   # path to your RSA private key file, OR the key content

# Scanner settings
KALSHI_ENV=prod          # prod | demo
RESOLVE_DAYS=7                  # only scan markets closing within this many days
MIN_SPREAD_THRESHOLD=0.10       # flag markets with bid-ask spread >= this (e.g. 0.10 = 10 cents)
MAX_MARKETS=10000               # stop fetching after this many markets
MIN_VOLUME=1                    # exclude markets with 24h volume below this (0 = include all)
MAX_VOLUME_THRESHOLD=500        # flag markets with 24h volume <= this (low liquidity)
MIN_ANOMALY_SCORE=0.5           # minimum anomaly score (0-1) to include in report
OUTPUT_DIR=./output             # where to write daily scan reports
